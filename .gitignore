__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
env/
venv/
ENV/

# 操作系统、编辑器和 Office 临时文件。
.DS_Store
Thumbs.db
*.swp
*.swo
*~
~$*
.~*

# 本地工具配置目录。
.cursor/
.vscode/
.claude/
.codex/

# 打包产物。
*.egg-info/
build/
dist/

.env
.env.*
!.env.example

data/
chroma_db/
*.pkl
*.bin
*.sqlite3

logs/
*.log
backups/

测试论文/
your_documents/
artifacts/local-docs/

# 真实评测集绑定私有语料名与本机索引标识，不入库；只提交匿名样例。
eval/*.jsonl
!eval/*.example.jsonl
eval/eval_suite_report.json
eval/eval_suite_baseline.json
eval/retrieval_eval_report.json
eval/retrieval_eval_baseline.json
eval/retrieval_gate.json

# Reliability workbench state and generated reports can contain local paths,
# private corpus copies, queries, answers, and retrieved source text. Keep only
# deliberately reviewed baseline/calibration summaries eligible for commits.
artifacts/reliability/eval-data/
artifacts/reliability/*.json
artifacts/reliability/*.jsonl
!artifacts/reliability/*-baseline.json
!artifacts/reliability/*-calibration.json
!artifacts/reliability/confidence-calibration-v3.json

# Node / mermaid-cli 本地渲染依赖。
node_modules/
.next/
playwright-report/
test-results/
*.tsbuildinfo
